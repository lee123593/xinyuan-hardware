{
  "buildCommand": "cd backend && npm install",
  "outputDirectory": ".",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/main" }
  ],
  "functions": {
    "api/main.js": {
      "includeFiles": "backend/**"
    }
  }
}
